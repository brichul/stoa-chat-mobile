# Base URL of the Stoa backend (FastAPI). Must be reachable from wherever the
# app runs: the Mac's LAN IP works for both the iOS simulator and a physical
# device on the same Wi-Fi. Keep in sync with the backend's API_BASE_URL.
EXPO_PUBLIC_API_URL=http://192.168.2.43:8000
